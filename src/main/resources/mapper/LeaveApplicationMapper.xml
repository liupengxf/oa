<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.buu.oa.mapper.LeaveApplicationMapper">

    <select id="selectPendingByDeptId" resultType="org.buu.oa.entity.LeaveApplication">
        SELECT la.* FROM leave_application la
        INNER JOIN emp_employee e ON la.emp_id = e.id
        WHERE e.dept_id = #{deptId} AND la.status = 'PENDING'
        ORDER BY la.create_time DESC
    </select>

</mapper>