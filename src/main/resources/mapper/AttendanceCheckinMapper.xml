<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.buu.oa.mapper.AttendanceCheckinMapper">

    <select id="selectByEmpIdAndDateRange" resultType="org.buu.oa.entity.AttendanceCheckin">
        SELECT * FROM attendance_checkin
        WHERE emp_id = #{empId} AND check_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY check_date DESC
    </select>

</mapper>